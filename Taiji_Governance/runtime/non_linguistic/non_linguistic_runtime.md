# Non-Linguistic Tensor Runtime

Language is an interface, not the runtime core.

Runtime state is represented by:

- TensorPacket
- topology vector
- authority vector
- device state
- deployment state
- audit state
- sensor/event state

Natural language must be converted into tensor state before any action.
