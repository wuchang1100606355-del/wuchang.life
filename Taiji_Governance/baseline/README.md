# Baseline Directory

This directory is reserved for SHA256 manifests and rollback references.

Rules:

- Do not store secrets here.
- Do not store service account JSON files here.
- Do not store OAuth tokens here.
- Store hashes, file paths, timestamps, and rollback notes only.
