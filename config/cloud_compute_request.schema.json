{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "8D_CANDIDATE_COMPLETION_PACKET",
  "description": "W7TP Strict Schema for Cloud Compute Candidate. Enforces pure JSON Delta output.",
  "type": "object",
  "properties": {
    "patch_type": {
      "type": "string",
      "enum": ["CREATE", "UPDATE", "DELETE", "NO_CHANGE"],
      "description": "The mutation intent of the delta."
    },
    "file_ref": {
      "type": "string",
      "description": "Relative path to the target file."
    },
    "content_delta": {
      "type": "string",
      "description": "The exact code or text to be applied. If values are unknown/unauthorized, must contain <W7TP_REDACTED>."
    }
  },
  "required": [
    "patch_type",
    "file_ref",
    "content_delta"
  ],
  "additionalProperties": false
}
