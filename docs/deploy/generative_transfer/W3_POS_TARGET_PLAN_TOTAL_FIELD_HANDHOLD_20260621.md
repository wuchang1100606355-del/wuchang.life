# W3 POS Target Plan Total Field Handhold

RUN_ID=W3_POS_TARGET_PLAN_TOTAL_FIELD_HANDHOLD_20260621
STATE=TARGET_PLAN_TOTAL_FIELD_HANDHOLD

## STATE
FACT: Host authority is `taiji01` Total Field.

FACT: Current repository root is `/home/taiji_admin/Taiji_Hub`.

FACT: W3 Generative Transfer Deploy is the required next trunk before runtime action, UI scaffold, broker, connector, browser action, Odoo write, LINE WORKS send, Tailscale mutation, or customer display live control.

FACT: Cloud compute may be triggered only as bounded labor. It has no authority, no memory benefit, no data benefit, no Land authority, and returns candidate packets only.

FACT: Safety flags for this plan are `NO_DEPLOY`, `NO_RESTART`, `NO_DB_WRITE`, `NO_SECRET_READ`, `NO_MEMBER_PLAINTEXT_READ`, `NO_PROD_LINEWORKS_SEND`, `NO_GOOGLE_ACTION`, and `NO_ODOO_MUTATION`.

SIMPLIFICATION_MISSED_ALERT: Not triggered because this plan uses W7TP, GT8D route lookup, 8D packet identity, Spacetime hash ledger, local reconstruction, and evidence gates as the shortest path.

## TOTAL_FIELD_FACTS
FACT: Target product domain is `上品聊國咖啡館重新總店` POS, ERP, member AI service, and store operation maintenance.

FACT: Existing Total Field convergence evidence identifies Odoo POS as the core business system design surface and PostgreSQL as the persistence layer in design documents.

FACT: Existing convergence evidence identifies a POS naming collision: POS ID 3 and POS ID 4 share the same cafe POS name in Drive reports.

FACT: Existing convergence evidence requires strict scope separation between `上品食品行 / 重新總店` and `五常社區發展協會 / 仁義分店`.

FACT: The safe candidate chain is `menu.json -> customer_order_candidate -> GT8D route lookup -> verifier -> cashier confirm -> local order log / Odoo sidecar candidate -> kitchen display -> spacetime audit -> dead_letter_queue on failure`.

FACT: `NODE_POS_MAINT` is modeled as the POS maintenance node.

FACT: `NODE_XIAOJ_DISPLAY_COMPUTE` is modeled as Linux compute, Chrome customer display, and external TV XiaoJ image display node.

FACT: Tailscale invite links are access-bearing secrets and must not be written to docs, fixtures, logs, or packets.

FACT: LINE WORKS notification is candidate notice only until local entitlement, risk gate, staff confirmation, and final authorization are complete.

FACT: Cloud compute is not forbidden. Cloud compute is assigned hard non-authoritative labor only.

## TOTAL_FIELD_GAPS
INFO_REQUIRED: Live Odoo runtime status is not verified.

INFO_REQUIRED: Current PostgreSQL database state is not verified.

INFO_REQUIRED: Current Store Apprentice process status is not verified.

INFO_REQUIRED: Current Sunmi or POS hardware identity binding is not verified.

INFO_REQUIRED: Current Chrome customer display device identity is not verified.

INFO_REQUIRED: Current LINE WORKS app, tenant, bot, or webhook configuration is not verified.

INFO_REQUIRED: Current Google registration and Odoo binding details are not verified.

NOT_YET_VERIFIED: POS ID 3/4 same-name collision cause is not known.

NOT_YET_VERIFIED: 64-gua, bitmask, ancient mathematics, or metaphysical mapping is design-proposal material only and must not be treated as completed product evidence.

INFO_REQUIRED: Total Field must choose the first minimal runnable module before any runtime-facing action plan proceeds.

## PRODUCT_TARGET_ARCHITECTURE
DESIGN_PROPOSAL: Product position is a sovereign edge AI POS operations layer for Odoo stores, not a generic chatbot, generic SaaS POS, or browser automation product.

DESIGN_PROPOSAL: Core value is candidate generation plus local reconstruction: cloud and AI produce candidate packets; taiji01, GT8D lookup, verifier, ledger, and human gate decide.

DESIGN_PROPOSAL: Store flow is `customer/staff intent -> local AV/menu packet -> order_candidate -> route packet -> local verifier -> cashier confirmation -> Odoo sidecar candidate -> kitchen/customer display candidate -> Spacetime audit`.

DESIGN_PROPOSAL: Member service flow is `member intent -> 8D identity code -> service entitlement -> route packet -> LINE WORKS candidate notice -> local risk gate -> send HOLD or authorized action`.

DESIGN_PROPOSAL: Cloud labor flow is `redacted 8D request -> cloud batch candidate -> local schema verification -> local reconstruction -> evidence or HOLD`.

DESIGN_PROPOSAL: Dual-node split keeps POS maintenance and display/compute separated so a display or AV workload cannot directly mutate POS records.

DESIGN_PROPOSAL: Product-grade moat is the verifiable sequence `State -> Coordinate -> Hash -> Packet -> Generative Transfer -> Verify -> Reconstruct -> Evidence -> Action`.

NOT_YET_VERIFIED: Market readiness cannot be claimed until demo proof, ROI measurements, live device inventory, and Odoo read-only probes are completed.

## SCHEMA_TARGETS
FACT: `w7tp_8d_identity_code` binds member, merchant, node, device, and entitlement references without member plaintext.

FACT: `w7tp_store_node_profile` models `NODE_POS_MAINT` and `NODE_XIAOJ_DISPLAY_COMPUTE` roles, capabilities, risk tier, and display target.

FACT: `w7tp_gt8d_route_packet` carries route code, lookup key, candidate hash, and local reconstruction requirement.

FACT: `w7tp_spacetime_event` carries event id, parent hash, actor reference, node reference, action reference, claim label, and event hash.

FACT: `w7tp_pos_order_candidate` models AI/AV order candidates that cannot directly create Odoo orders.

FACT: `w7tp_service_entitlement` models `FREE`, `ULTRA`, `GROUP_MEMBER`, `ADMIN`, and `FOUNDER_BREAK_GLASS`.

FACT: `w7tp_total_field_review_packet` records Total Field review recommendation, risk, evidence references, human action, and reseal requirement.

FACT: `w7tp_cloud_compute_resilience_gate` records cloud labor-only controls.

FACT: `w7tp_user_stance_integrity_gate` records user stance priority, label rules, unified question rule, and stopline.

DESIGN_PROPOSAL: Next schema target is `w7tp_customer_order_candidate_api` only if Total Field chooses option A.

## MINIMUM_RUNNABLE_MODULES
DESIGN_PROPOSAL: Module A `POS order_candidate API` accepts redacted menu and order intent and emits `w7tp_pos_order_candidate` only.

DESIGN_PROPOSAL: Module B `cashier_confirm gate` verifies candidate hash, entitlement, staff role, and risk tier before any sidecar landing.

DESIGN_PROPOSAL: Module C `kitchen_display` reads confirmed local candidate state and emits display-only packets.

DESIGN_PROPOSAL: Module D `spacetime_event wrapper` records every candidate, verification, confirmation, HOLD, and dead-letter step in a hash chain.

DESIGN_PROPOSAL: Module E `Odoo sidecar candidate` writes no Odoo core state and stores only local dry-run candidate evidence until Stage 2 authorization.

DESIGN_PROPOSAL: Module F `8D identity binding skeleton` binds member, merchant, device, node, and entitlement references without plaintext member behavior.

DESIGN_PROPOSAL: Module G `GT8D route table POS expansion` adds route packets for POS, LINE WORKS candidate notice, XiaoJ display compute, and dead-letter routing.

FACT: The current safest runnable level remains schema, fixture, route lookup, verifier, and dry-run evidence.

## AUTOGEN_PLAN
DESIGN_PROPOSAL: Step 1 reads Total Field evidence and writes no runtime state.

DESIGN_PROPOSAL: Step 2 seals W3 deploy package, Total Field review packet, target plan, schema fixtures, route table changes, and hash evidence.

DESIGN_PROPOSAL: Step 3 uses cloud compute only for redacted competitor, redteam, schema candidate, and stress scenario labor.

DESIGN_PROPOSAL: Step 4 validates every cloud result locally for schema, claim labels, forbidden fields, route integrity, and capacity controls.

DESIGN_PROPOSAL: Step 5 reconstructs candidate outputs locally through GT8D lookup and Spacetime event ledger.

DESIGN_PROPOSAL: Step 6 emits `STATE=HOLD_MAIN_CHAIN_DEVIATION` if any step tries to jump from evidence/spec/dry-run to action.

DESIGN_PROPOSAL: Step 7 waits for Total Field to choose A-G before generating the first runnable module implementation.

## DO_NOT_DO
FACT: Do not redeem, open, print, store, or validate Tailscale invite links.

FACT: Do not read secrets, member plaintext, access tokens, refresh tokens, private keys, or live credential paths.

FACT: Do not write Odoo, PostgreSQL, POS, LINE WORKS, Google, router, Tailscale, Chrome customer display, or production service state.

FACT: Do not claim live Odoo status, live device status, POS ID collision cause, ROI, patent proof, market dominance, or implemented unverified mapping without evidence.

FACT: Do not replace the W7TP main chain with ordinary UI, ordinary chatbot, ordinary SaaS workflow, or generic browser automation.

FACT: Do not let cloud compute keep memory, benefit from data, make authority decisions, or Land candidate results.

FACT: Do not split missing facts into scattered assumptions. Preserve them under `NEXT_TOTAL_FIELD_QUERY`.

## NEXT_TOTAL_FIELD_QUERY
INFO_REQUIRED: 請總場指定第一個最小可安全落地模組：

A. POS order_candidate API

B. cashier_confirm gate

C. kitchen_display

D. spacetime_event wrapper

E. Odoo sidecar candidate

F. 8D identity binding skeleton

G. GT8D route table POS expansion

INFO_REQUIRED: 若選 A-G 以外項目，請總場先標示該項目在主鏈中的位置：State / Coordinate / Hash / Packet / Generative Transfer / Verify / Reconstruct / Evidence / Action。
