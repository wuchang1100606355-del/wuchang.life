# PM3 System Status Inventory

Source prompt: GitHub issue `o970106-dev/123#131`

Generated: 2026-05-18 Asia/Taipei

## Executive Summary

This is a source-only inventory report. No Odoo install, app update, container restart, database mutation, `ir_model` edit, secret access, commit, live deploy, or module rewrite was performed.

The issue prompt targets `/home/user/o970106-dev/123`, but that path is not present on this machine. The scan was therefore applied to the current canonical workspace:

`/home/taiji_admin/Taiji_Hub`

Active addon root used for this inventory:

`/home/taiji_admin/Taiji_Hub/Taiji_Odoo/addons`

Summary decision:

- `INSTALL_ALLOWED = NO`
- `PM3_BASE_REVIEW = BLOCKED`
- `overall_pm3_property_progress = 34 / 100`

The active addon root is small and understandable, but it is not clean enough for install promotion. `pm3_base` exists, but it is incomplete relative to the issue prompt and has a high-risk public ingest controller.

## Repository Path Check

Requested repo path:

`/home/user/o970106-dev/123`

Result:

`MISSING:/home/user/o970106-dev/123`

Actual scanned root:

`/home/taiji_admin/Taiji_Hub`

Git status evidence:

```text
 M main.py
?? Taiji_Odoo/
?? _imports/
?? reports/
...many untracked workspace paths...
```

Git diff stat before this report:

```text
 main.py | 25 -------------------------
 1 file changed, 25 deletions(-)
```

Interpretation:

The working tree is broad and dirty, with many untracked paths. This does not by itself block source inventory, but it blocks any claim of clean promotion readiness.

## Active Addons Topology

Directories found under `Taiji_Odoo/addons`:

```text
Taiji_Odoo/addons
Taiji_Odoo/addons/pm3_base
Taiji_Odoo/addons/pm3_base/controllers
Taiji_Odoo/addons/pm3_base/models
Taiji_Odoo/addons/pm3_base/security
Taiji_Odoo/addons/pm3_base/views
Taiji_Odoo/addons/wuchang_cafe_menu_options
Taiji_Odoo/addons/wuchang_cafe_menu_options/models
Taiji_Odoo/addons/wuchang_cafe_menu_options/security
Taiji_Odoo/addons/wuchang_cafe_menu_options/views
Taiji_Odoo/addons/wuchang_core
Taiji_Odoo/addons/wuchang_core/models
Taiji_Odoo/addons/wuchang_core/security
Taiji_Odoo/addons/wuchang_core/views
```

Manifests found:

```text
Taiji_Odoo/addons/pm3_base/__manifest__.py
Taiji_Odoo/addons/wuchang_cafe_menu_options/__manifest__.py
Taiji_Odoo/addons/wuchang_core/__manifest__.py
```

Active addons:

| Module | Present | Install status from prior DB evidence | Inventory decision |
|---|---:|---|---|
| `pm3_base` | yes | not installed in `clean18` | blocked for review |
| `wuchang_core` | yes | uninstalled in `odoo` | not install-ready |
| `wuchang_cafe_menu_options` | yes | uninstalled | blocked by missing dependency |

## Quarantine Status

No active addon quarantine directory was found under `Taiji_Odoo/addons`:

```text
find Taiji_Odoo/addons -maxdepth 2 -type d -name '_quarantine*' -o -name '*quarantine*'
```

Result:

```text
<empty>
```

Important boundary:

The workspace-level `_imports` directory exists and remains a review lane only. It is not part of active addons.

## Contamination Scan

Search for known bad PM3 names:

```text
pm3_integrated_property
pm3_integrations
pm_base
pm_identity
```

Result in active addons:

```text
<empty>
```

Source artifact contamination found:

```text
Taiji_Odoo/addons/pm3_base/controllers/__pycache__/edge_api.cpython-312.pyc
Taiji_Odoo/addons/pm3_base/controllers/edge_api.py.bak
Taiji_Odoo/addons/pm3_base/models/__pycache__/packet_history.cpython-312.pyc
Taiji_Odoo/addons/wuchang_cafe_menu_options/models/__pycache__/menu_options.cpython-312.pyc
Taiji_Odoo/addons/wuchang_core/models/__pycache__/wuchang_matrix.cpython-312.pyc
```

Severity:

`B2 = module topology issue`

Reason:

Active addons contain generated bytecode and backup files. These should not be in a clean addon review surface.

## Deprecated Odoo Syntax Scan

`@api.multi` scan:

```text
grep -R '@api.multi' -n Taiji_Odoo/addons || true
```

Result:

```text
<empty>
```

Legacy XML scan:

```text
grep -R '<tree\| states=\|attrs=' -n Taiji_Odoo/addons || true
```

Result:

```text
Taiji_Odoo/addons/wuchang_core/views/wuchang_views.xml:8:            <tree>
```

Severity:

`B3 = Odoo registry/install blocker`

Reason:

Odoo 18 official addon XML uses `<list>` for list views. A legacy `<tree>` tag should be corrected before install review.

## Invalid Model Name Scan

Model declarations found:

```text
wuchang.no.delete.mixin
wuchang.wish.project
wuchang.bank.clearance
wuchang.ai.verification
wuchang.renyi.volunteer.business
wuchang.renyi.volunteer.account
wuchang.renyi.volunteer.account.line
wuchang.volunteer.management.meeting
wuchang.task.force.dispatch
pm3.packet.history
wuchang.cafe.option.group
wuchang.cafe.option.question
wuchang.cafe.option.item
```

No obviously invalid dotted model names were found in active addons.

Inheritance declarations found:

```text
pos.order
product.template
wuchang.no.delete.mixin
```

Risk:

`pos.order` and `product.template` inheritance require the corresponding dependencies to be installed and declared correctly.

## pm3_base File Completeness

Files found:

```text
Taiji_Odoo/addons/pm3_base/__init__.py
Taiji_Odoo/addons/pm3_base/__manifest__.py
Taiji_Odoo/addons/pm3_base/controllers/__init__.py
Taiji_Odoo/addons/pm3_base/controllers/__pycache__/edge_api.cpython-312.pyc
Taiji_Odoo/addons/pm3_base/controllers/edge_api.py
Taiji_Odoo/addons/pm3_base/controllers/edge_api.py.bak
Taiji_Odoo/addons/pm3_base/models/__init__.py
Taiji_Odoo/addons/pm3_base/models/__pycache__/packet_history.cpython-312.pyc
Taiji_Odoo/addons/pm3_base/models/packet_history.py
Taiji_Odoo/addons/pm3_base/security/ir.model.access.csv
```

Manifest:

```python
{
    'name': 'PM3 Base',
    'version': '1.0',
    'summary': 'PM3 Governance Base',
    'category': 'Governance',
    'license': 'LGPL-3',
    'depends': ['base'],
    'data': [
        'security/ir.model.access.csv',
    ],
    'installable': True,
    'application': True,
}
```

Access CSV:

```text
id,name,model_id:id,group_id:id,perm_read,perm_write,perm_create,perm_unlink
access_pm3_packet_history_portal,pm3.packet.history.portal,model_pm3_packet_history,base.group_portal,1,0,0,0
access_pm3_packet_history_system,pm3.packet.history.system,model_pm3_packet_history,base.group_system,1,1,1,1
```

Expected by issue prompt but missing:

```text
Taiji_Odoo/addons/pm3_base/models/pm3_base_config.py
```

Observed model:

`pm3.packet.history`

Fields:

- `packet_id`
- `node`
- `source_layer`
- `target_layer`
- `five_d_code`
- `governance_level`
- `memory_scope`
- `audit_hash`
- `payload_hash`
- `timestamp`

Severity:

`B2 = module topology issue`

## pm3_base Odoo18 Compliance

Positive evidence:

- No `@api.multi`.
- Model naming is valid.
- Access CSV exists.
- Manifest is syntactically simple.

Blocking evidence:

- `controllers/edge_api.py` defines public unauthenticated JSON route:
  - `auth='public'`
  - `csrf=False`
  - `sudo().create(...)`
- The controller writes directly into `pm3.packet.history`.
- Required fields are not explicitly validated before `sudo().create`.
- Backup and bytecode files exist inside active module path.

Controller risk excerpt:

```python
@http.route(
    '/pm3/tensor_ingest',
    type='json',
    auth='public',
    methods=['POST'],
    csrf=False
)
...
packet = request.env['pm3.packet.history'].sudo().create({...})
```

Severity:

`B3 = Odoo registry/install blocker`

Reason:

A public unauthenticated endpoint with `sudo().create()` is not acceptable for install promotion without gateway policy, payload validation, rate/risk control, and audit constraints.

## Module Completion Matrix

| Module | Source completeness | Odoo 18 syntax | Dependency hygiene | Security posture | Install readiness |
|---|---:|---:|---:|---:|---:|
| `pm3_base` | 60 | 70 | 65 | 25 | 35 |
| `wuchang_core` | 55 | 35 | 55 | 45 | 30 |
| `wuchang_cafe_menu_options` | 50 | 60 | 20 | 45 | 20 |
| property lane | 35 | 20 | 15 | 35 | 10 |

Notes:

- `wuchang_cafe_menu_options` depends on `wuchang_cafe_ai_gateway`, which is not present in active addons.
- Property management is not present in active addons; it exists only as an `_imports` candidate reviewed separately.

## Gate Status

| Gate | Result | Evidence |
|---|---|---|
| Active `pm3_integrated_property` exists | PASS | scan empty |
| Contamination scan nonempty | FAIL | `.pyc` and `.bak` files present |
| Deprecated Odoo syntax | FAIL | `<tree>` in `wuchang_core` |
| `pm3_base` required files complete | FAIL | `pm3_base_config.py` missing |
| Public unsafe controller absent | FAIL | `auth='public'`, `csrf=False`, `sudo().create` |
| PM3 base ready for governance review | NO | blockers remain |
| Install allowed | NO | source not clean |

## Quantified Progress

| Metric | Score |
|---|---:|
| architecture_layer | 72 |
| governance_layer | 62 |
| execution_brain_layer | 50 |
| active_addons_cleanliness | 48 |
| pm3_base_file_completeness | 55 |
| pm3_base_odoo18_compliance | 52 |
| pm3_base_install_readiness | 30 |
| overall_pm3_property_progress | 34 |

Weighted interpretation:

The system has a coherent architecture direction, but source cleanliness and install-readiness are substantially lower than conceptual maturity.

## Critical Blockers

1. `B3` public write endpoint in `pm3_base`
   - Evidence: `auth='public'`, `csrf=False`, `sudo().create`.
   - Impact: install promotion should remain blocked.

2. `B3` Odoo 18 XML compatibility issue in active addons
   - Evidence: `wuchang_core/views/wuchang_views.xml` contains `<tree>`.
   - Impact: Odoo 18 view validation risk.

3. `B2` active addon source contamination
   - Evidence: `.pyc` and `.bak` files in active addon tree.
   - Impact: review surface is not clean.

4. `B2` `pm3_base` file completeness mismatch
   - Evidence: issue prompt expects `models/pm3_base_config.py`; file is absent.
   - Impact: cannot claim PM3 base completeness.

5. `B3` missing dependency for `wuchang_cafe_menu_options`
   - Evidence: manifest depends on `wuchang_cafe_ai_gateway`; module is not present.
   - Impact: module cannot be installed cleanly as-is.

## Next 3 Actions

1. Clean the active addon source surface in a source-only change:
   - remove `.pyc` files
   - remove or quarantine `.bak` files
   - keep runtime data outside addon tree

2. Prepare `pm3_base` governance review patch:
   - replace public `sudo().create()` route with governed authentication or Claw Safe gateway boundary
   - add payload validation
   - define whether `pm3_base_config.py` is required or update the PM3 spec

3. Run a disposable Odoo 18 source/install readiness pass:
   - convert legacy `<tree>` to `<list>`
   - resolve `wuchang_cafe_ai_gateway`
   - verify manifests and access files before touching `clean18` or `odoo`

Final flag:

`PM3_INVENTORY_DONE`
