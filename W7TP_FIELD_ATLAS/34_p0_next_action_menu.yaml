decision_id: 34_p0_next_action_menu
purpose: Human-selectable next action menu for fastest controlled development.
mode: docs_index_only
options:
  A_state_router_spec: build State World Router spec from existing indexes
  B_memory_boot_map: connect memory lifecycle to boot/readiness docs
  C_gateway_inventory: classify gateway files into route bridge health startup config
  D_odoo_inventory: classify Odoo modules into commerce member fund delivery POS
recommended_order:
  - A_state_router_spec
  - B_memory_boot_map
  - C_gateway_inventory
  - D_odoo_inventory
governance:
  docs_only: true
  no_secret_read: true
  no_runtime_change: true
next_action: choose A B C or D
