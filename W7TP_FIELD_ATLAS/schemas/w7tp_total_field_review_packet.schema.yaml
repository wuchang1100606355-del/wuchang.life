schema_id: W7TP_TOTAL_FIELD_REVIEW_PACKET
status: DRAFT
required_fields:
  - review_id
  - request_type
  - risk_level
  - evidence_refs
  - xiaoj_review_opinion
  - required_human_action
  - expiry_or_reseal_required
xiaoj_review_opinion_fields:
  - recommendation
  - reason
  - safe_option
  - risk_if_approved
  - risk_if_rejected
  - required_human_action
allowed_recommendations:
  - approve_limited
  - reject
  - defer
  - require_more_evidence
security_boundary:
  no_secret: true
  no_member_plaintext: true
  no_runtime_authorization: true

