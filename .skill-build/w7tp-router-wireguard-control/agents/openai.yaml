interface:
  display_name: "W7TP Router WireGuard Control"
  short_description: "8DADI-managed RT-BE86U dual-stack WireGuard and LAN/DNS control"
  default_prompt: "Use $w7tp-router-wireguard-control to observe or prepare one exact RT-BE86U adjustment through the existing Total Field router chain; require separate D8 authority before any network effect."
