map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

server {
    listen 8088;
    server_name odoo.wuchang.life;

    location / {
        proxy_pass http://host.docker.internal:8069;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto http;
    }
}

server {
    listen 8088;
    server_name ai.wuchang.life;

    location / {
        proxy_pass http://host.docker.internal:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
    }
}

server {
    listen 8088;
    server_name xiaoj.wuchang.life;

    location / {
        proxy_pass http://host.docker.internal:9107;
        proxy_set_header Host $host;
    }
}

server {
    listen 8088;
    server_name gateway.wuchang.life;

    location / {
        proxy_pass http://host.docker.internal:8081;
        proxy_set_header Host $host;
    }
}
